---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart ehpanel webmail
  ansible.builtin.systemd:
    name: "{{ ehpanel_webmail_service_name }}"
    state: restarted
    enabled: true
    daemon_reload: true
