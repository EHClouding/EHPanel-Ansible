---
- name: reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
  when: not ansible_check_mode

- name: reload httpd
  ansible.builtin.systemd:
    name: httpd
    state: restarted
  when: not ansible_check_mode
