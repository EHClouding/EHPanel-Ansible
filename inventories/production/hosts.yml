---
# inventories/production/hosts.yml
# Hosts de producción.
# IPs reales en host_vars/<host>/vault.yml (cifrado con ansible-vault).
# Agregar hosts aquí a medida que se aprovisionen.

all:
  vars:
    inventory_environment: production

  children:

    web_nodes:
      hosts:
        web-01.ehclouding.com:
          ansible_host: 184.107.179.221
      # Agregar nodos de producción aquí:
      # web01.ehclouding.com:
      # web02.ehclouding.com:

    radio_nodes:
      hosts: {}

    video_nodes:
      hosts: {}

    srt_nodes:
      hosts: {}

    all_managed_nodes:
      children:
        web_nodes:
        radio_nodes:
        video_nodes:
        srt_nodes:
